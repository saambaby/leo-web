## Goal
Implement P1-2 foundation: switch-tenant BFF, useMemberships hook, permission codegen, last_tenant_id localStorage sync.

## Owns
- `app/api/auth/switch-tenant/route.ts`
- `lib/hooks/use-memberships.ts` (or similar)
- `lib/permissions/generated.ts`, `scripts/codegen-permissions.ts`, `package.json` codegen script
- `components/permission-gate.tsx`, `lib/permissions/has-permission.ts`
- `lib/last-tenant.ts` + AuthProvider/login integration for auto-switch

## Must-not-touch
- AdminShell / layout chrome (P1-2-T-02)
- Switch-tenant modal UI (P1-2-T-03)
- ProtectedGuard MFA upgrades (P1-2-T-02)

## Integration contract
- Inherited P1-1: :8080, /api/v1 proxy, snake_case, leo_refresh httpOnly, CSRF on BFF POSTs
- NEW: GET /memberships (Bearer); POST /api/auth/switch-tenant BFF
- NEW: localStorage leo.last_tenant_id
- NEW: npm run codegen:permissions → lib/permissions/generated.ts

## Success criteria
- [ ] BFF switch-tenant with CSRF; refresh from cookie server-side
- [ ] mfa_required handled without setSession
- [ ] useMemberships hook with nested organization types
- [ ] Permission codegen + hasPermission + PermissionGate default-deny
- [ ] last_tenant_id persist + auto-switch on login
- [ ] npm run lint && npm run build pass

## Dispatch prompt
Implement **P1-2-T-01** for leo-web. Read `.pineapple/features/p1-2-protected-shell.md`. Branch: `pin-<issue>/p1-2-t01-foundation` on main (foundation — coordinator branch, no worktree). Run lint/build. Open PR linking this issue.

## References
- Parent: (set after create)
- Feature spec: `.pineapple/features/p1-2-protected-shell.md`
- Depends on: P1-1 complete
