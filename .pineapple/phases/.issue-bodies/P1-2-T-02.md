## Goal
Build AdminShell light-theme chrome, upgrade ProtectedGuard for session+MFA, wire sign-out.

## Owns
- `components/admin-shell.tsx`
- `components/protected-guard.tsx` (upgrade)
- Protected layout files in `app/(platform)`, `(lsp)`, `(portal)`, `(account)`

## Must-not-touch
- BFF switch-tenant internals (P1-2-T-01)
- Switch-tenant modal (P1-2-T-03)
- PermissionGate on nav (P1-2-T-03)

## Integration contract
Same as P1-1 + P1-2-T-01. Sign-out uses existing BFF logout.

## Success criteria
- [ ] Unauthenticated → /login?returnTo=
- [ ] Privileged without MFA → /mfa/enroll
- [ ] AdminShell header/sidebar/nav/sign-out
- [ ] All protected layouts use AdminShell
- [ ] npm run lint && npm run build pass

## Dispatch prompt
Implement **P1-2-T-02** on branch `pin-<issue>/p1-2-t02-admin-shell`. Worktree per task. PR links this issue.

## References
- Depends on: P1-2-T-01 merged
