## Goal
Wire auth flows end-to-end: role routing, login/MFA, invite accept, admin setup, invariant promotions.

## Owns
- `lib/auth-routing.ts`
- `app/login/**`, `app/mfa/**`, `app/invite/accept/**`, `app/admin/setup/**`
- `.pineapple/invariants.md` (promote INV-WEB-AUTH-1/2/3, ROUTE-2/3/4, MFA-1, UI-2)

## Must-not-touch
- BFF route handler internals unless required for flow integration
- Design-system component definitions (P1-1-T-02)
- Route group structure (P1-1-T-03) except page content in new routes

## Integration contract
Same as P1-1 parent (#1). Invite accept must NOT call `setSession`. Role routing per product spec §4.

## Success criteria
- [ ] `lib/auth-routing.ts` maps JWT `role` → home; missing `tenant_id` → `/account`
- [ ] Login inline TOTP on `mfa_required`; success calls `setSession` + `routeAfterLogin`
- [ ] `/mfa` accepts TOTP with `?returnTo=` without breaking inline login
- [ ] `/invite/accept` → `/login?invited=1` without `setSession`
- [ ] `/admin/setup` funnel → MFA enroll → `/admin/platform` stub
- [ ] MFA enrollment pending in AuthProvider memory
- [ ] Invariants updated
- [ ] `npm run build` and `npm run lint` pass

## Dispatch prompt
You are implementing **P1-1-T-04** for leo-web. Read `.pineapple/features/p1-1-session-auth-foundation.md` and `.pineapple/product-spec.md` §4. Wire login/MFA/invite/setup flows, add `lib/auth-routing.ts`, update invariants. Branch: `pin-5/p1-1-t04-auth-flows`. Run `npm run lint` and `npm run build`. Open PR linking this issue. Manual E2E checklist in issue body for operator sign-off.

## References
- Parent: #1
- Depends on: P1-1-T-01, P1-1-T-02, P1-1-T-03 merged
- Feature spec: `.pineapple/features/p1-1-session-auth-foundation.md`
