## Goal
Switch-tenant modal in AdminShell header, PermissionGate on nav, /account security UI. Closes P1 spine.

## Owns
- `components/switch-tenant-modal.tsx`
- AdminShell header switch slot + nav PermissionGate
- `app/(account)/account/page.tsx` security section

## Must-not-touch
- BFF route internals unless wiring modal
- AdminShell structure beyond header/nav slots (coordinate with T02)

## Integration contract
Uses T01 BFF switch-tenant, useMemberships, PermissionGate, routeAfterLogin.

## Success criteria
- [ ] Switch visible when memberships > 1
- [ ] Modal full-row buttons; MFA → /mfa?returnTo=
- [ ] PermissionGate denies cross-role nav
- [ ] /account security + interpreter empty state
- [ ] Invariants promoted
- [ ] npm run lint && npm run build pass

## Dispatch prompt
Implement **P1-2-T-03** on branch `pin-<issue>/p1-2-t03-switch-account`. Worktree. Manual E2E checklist in PR. Closes P1 spine.

## References
- Depends on: P1-2-T-01, P1-2-T-02 merged
