## Goal
Restructure App Router into layout groups with protected stub pages and WorkstationCta component.

## Owns
- `app/(public)/**`, `app/(platform)/**`, `app/(lsp)/**`, `app/(portal)/**`, `app/(account)/**`
- `components/workstation-cta.tsx`
- Remove `app/dashboard/**`

## Must-not-touch
- BFF / AuthProvider internals (P1-1-T-01) except `useAuth()` consumer
- AuthShell visual migration (P1-1-T-02)
- Login flow wiring / role routing (P1-1-T-04)

## Integration contract
Same as P1-1 parent (#1). Use `useAuth()` from AuthProvider for minimal client guards.

## Success criteria
- [ ] Route groups `(public)`, `(platform)`, `(lsp)`, `(portal)`, `(account)`; public auth under `(public)`
- [ ] Stub pages at `/admin/platform`, `/admin/lsp`, `/portal/org`, `/portal/call`, `/account`
- [ ] Protected layouts redirect unauthenticated users to `/login` via `useAuth`
- [ ] WorkstationCta: native open/install copy; optional `leoconnexio://`; no `WORKSTATION_URL`
- [ ] `app/dashboard` removed
- [ ] `npm run build` and `npm run lint` pass

## Dispatch prompt
You are implementing **P1-1-T-03** for leo-web. Read `.pineapple/features/p1-1-session-auth-foundation.md`. Restructure `app/` into route groups, add protected stub pages with minimal `useAuth` guards, create WorkstationCta, remove `/dashboard`. Branch: `pin-4/p1-1-t03-router-stubs`. Run `npm run lint` and `npm run build`. Open PR linking this issue.

## References
- Parent: #1
- Depends on: P1-1-T-01 merged
- Feature spec: `.pineapple/features/p1-1-session-auth-foundation.md`
