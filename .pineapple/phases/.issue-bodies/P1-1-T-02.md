## Goal
Extract design-system primitives and migrate AuthShell + all public auth pages to `.theme-auth` token classes.

## Owns
- `components/design-system/` (Input, Button, Checkbox, Alert, Label)
- `components/auth-shell.tsx`
- Public auth route pages (visual only)

## Must-not-touch
- BFF / AuthProvider / lib/api.ts session logic (P1-1-T-01)
- Route groups / protected stubs (P1-1-T-03)
- Login flow wiring / role routing (P1-1-T-04)

## Integration contract
Same as P1-1 parent (#1). Do not change auth contract.

## Success criteria
- [ ] `components/design-system` exports Input, Button, Checkbox, Alert, Label using token classes
- [ ] AuthShell uses `.theme-auth` and `black-*` tokens; no hardcoded `#0b0d12`
- [ ] Public auth routes use design-system primitives
- [ ] `npm run build` and `npm run lint` pass

## Dispatch prompt
You are implementing **P1-1-T-02** for leo-web. Read `.pineapple/features/p1-1-session-auth-foundation.md`. Extract design-system primitives, migrate AuthShell to `.theme-auth`, update all public auth pages. Visual migration only — do not rewire session logic. Branch: `pin-3/p1-1-t02-design-system-auth`. Run `npm run lint` and `npm run build`. Open PR linking this issue.

## References
- Parent: #1
- Depends on: P1-1-T-01 merged
- Feature spec: `.pineapple/features/p1-1-session-auth-foundation.md`
